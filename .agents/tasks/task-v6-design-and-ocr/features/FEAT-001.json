{
  "id": "FEAT-001",
  "type": "chore",
  "description": "Set up the git branch and install dependencies to verify baseline works",
  "status": "completed",
  "steps": [
    "In /projects/sandbox/ufqbot, checkout main branch, then create and checkout new branch 'feat/v6-design-and-ocr'",
    "Install Python dependencies: cd test_grader_bot && pip install -r requirements.txt",
    "Run import verification: python -c 'from sheet_generator import generate_answer_sheet; from omr_scanner import scan_answer_sheet; print(\"Baseline OK\")'",
    "Generate a baseline test sheet to confirm it works: python -c 'from sheet_generator import generate_answer_sheet; img = generate_answer_sheet(30, 4); img.save(\"/tmp/baseline_v5.png\"); print(\"OK\")'"
  ],
  "acceptance_criteria": [
    "On branch feat/v6-design-and-ocr based off main",
    "All dependencies installed successfully",
    "Import verification passes without errors",
    "Baseline sheet generation produces a valid PNG file"
  ],
  "verification": [
    "Run: cd /projects/sandbox/ufqbot && git branch --show-current (should show feat/v6-design-and-ocr)",
    "Run: cd /projects/sandbox/ufqbot/test_grader_bot && python -c 'from sheet_generator import generate_answer_sheet; from omr_scanner import scan_answer_sheet; print(\"OK\")'"
  ],
  "blocked_reason": null,
  "findings": "System python is python3.9 at /usr/bin/python3.9. pyenv shim for 'python' is not configured, so always use python3.9 to run scripts. Dependencies installed to system python3.9 site-packages. Branch created from main (commit 1675a5c). The fix/omr-scanner-v5 branch has an additional commit c69cd4d that is NOT in main, so our branch starts from main without the v5 fix."
}
